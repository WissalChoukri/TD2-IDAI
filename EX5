#include <iostream>
using namespace std;
int main() {
    int a = 42;
    int &ref_a = a;
    int *p_a = &a;

   cout << "a: " << a << endl;
   cout << "ref_a: " << ref_a << endl;
   cout << "p_a (adresse): " << p_a << endl;
    cout << "p_a (valeur pointée): " << *p_a << endl;

    return 0;
}
